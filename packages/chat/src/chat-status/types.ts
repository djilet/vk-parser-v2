/**
 * Re-exported from @vk-sales-bot/contracts rather than redefined here — this value is
 * shared with apps/web, and a second definition is exactly how it would drift from the UI's.
 */
export { ChatStatus, DEFAULT_CHAT_STATUS, CHAT_STATUSES, isChatStatus } from '@vk-sales-bot/contracts';
